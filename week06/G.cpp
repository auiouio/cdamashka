#include <iostream>

using namespace std;

struct Atom
{
    string name;
    int A;
    int Z;
};

struct React
{
    string name;
    string reac;
};

void reaction ()

int main() {
    int N = 0, M = 0;
    cin >> N;
    Atom arr[N];
    for (int i = 0; i < N; i++) {
        cin >> (arr[i]).name >> (arr[i]).A >> (arr[i]).Z;
    }
    cin >> M;
    React brr[M];
    for (int i = 0; i < M; i++) {
        cin >> (brr[i]).name >> (brr[i]).reac;
        if ((brr[i]).reac == "beta-") {

        }
    }
    if ((brr[i]).reac == "beta-") 
    return 0;
}

